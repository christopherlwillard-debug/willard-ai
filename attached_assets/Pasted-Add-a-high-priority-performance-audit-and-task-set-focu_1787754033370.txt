Add a high-priority performance audit and task set focused on making the Media Center's NAS-backed browsing experience feel as fast as possible, comparable in responsiveness to optimized cloud photo storage such as Google Drive, while keeping the NAS as the permanent source of truth. Do not move the permanent family archive off the NAS. Audit and benchmark the entire path from storage to screen: NAS/SMB behavior, network throughput and latency, directory enumeration, metadata queries, thumbnail lookup, thumbnail file organization, database/index performance, concurrent reads, batching, lazy loading, virtualized grids, viewport prioritization, prefetching, memory cache, bounded local disk cache, thumbnail formats/sizes, preview loading, original-file access, CPU/GPU utilization, thumbnail generation parallelism, duplicate detection, resumability, and background processing. Determine the actual current bottlenecks rather than assuming the NAS is the only problem. Design tasks for a hybrid performance model: NAS permanently stores originals and durable library/thumbnail/preview knowledge; the client may use a small bounded disposable cache for speed; a stronger second laptop or future server may act as a processing/cache worker without becoming the sole source of any irreplaceable data. Make browsing retrieve stored thumbnails/metadata instead of reprocessing originals. Prioritize only the thumbnails visible on screen, preload likely next content, reuse previously generated results, and only regenerate artifacts when the original or processing version actually requires it. Include performance benchmarks and acceptance criteria so improvements can be measured before and after. Add safeguards against filling the Surface Pro 4's limited local storage. Create detailed tasks for every confirmed bottleneck/finding with severity, evidence, dependencies, acceptance criteria, and tests. Do not implement a broad redesign yet; add this to the ongoing audit/task roadmap.

Quoting the user: "i have noticed that things like google drive pops up very quickly which i know probably read and write alot faster than my MY cloud nas but i need to push the performance from it as much as possible when it comes to viewing and thumbnials and all of that"

